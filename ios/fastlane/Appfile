require('dotenv')
Dotenv.load '../../.env-cicd'

git_url(ENV['GIT_URL'])

app_identifier(ENV["APP_BUNDLE_ID"])
apple_id(ENV["APPLE_ID"])

itc_team_id(ENV["APPLE_ITC_TEAM_ID"])
team_id(ENV["APPLE_TEAM_ID"])
